// This file is overwritten by the build script with the inlined UI HTML.
// During development the CLI falls back to reading dist/ui/index.html from disk.
export const UI_HTML: string | null = null;
